node_modules/
android/app/build/
ios/build/
dist/
apps/web/dist/
.DS_Store

# Vite / local caches
apps/web/node_modules/.vite/

# Lighthouse CI output (regenerate via npm run lh:*)
.lighthouseci/
Lighthouse Reports/

# Firebase / local debug logs
firestore-debug.log
firebase-debug.log
firestore-debug.*.log

# Accidental shell pager captures (do not commit)
tatus
tash
tatus --porcelain
h origin main
; npm run build

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Local Netlify folder
.netlify

# Local secrets (never commit real values)
.env
.env.local
.env.*.local
.env.production
!.env.example

apps/web/.env
apps/web/.env.local
apps/web/.env.*.local
!apps/web/.env.example

# Service account credentials
**/flicklet-sa.json
server/flicklet-sa.json
functions/scripts/firebase-service-account-single-line.json

# Android signing credentials (contains passwords)
android/gradle.properties
