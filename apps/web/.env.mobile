# Mobile build configuration
# This file is used when building with --mode mobile
# Sets API_BASE to production backend URL for Capacitor Android/iOS builds

# Canonical site origin — makes verifyAuthEnvironment() recommend popup when WebView origin (localhost) differs
VITE_PUBLIC_BASE_URL=https://flicklet.netlify.app

# Web OAuth 2.0 client ID (ends in .apps.googleusercontent.com) from Google Cloud Console →
# APIs & Services → Credentials → OAuth 2.0 Client IDs → "Web client". Required for native Google Sign-In on Android/iOS.
VITE_GOOGLE_WEB_CLIENT_ID=1034923556763-em6vnddgb5dcu8dnai6poqb04i37m27a.apps.googleusercontent.com

VITE_API_BASE_URL=https://flicklet.netlify.app

# TMDB proxy base URL - must be absolute for Android WebView
# Defaults to /api/tmdb-proxy for web builds (works with Netlify redirects)
# For mobile, must be absolute URL to hit Netlify function directly
VITE_TMDB_PROXY_BASE=https://flicklet.netlify.app/api/tmdb-proxy
